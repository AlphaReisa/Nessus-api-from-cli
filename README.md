# Nessus-api-from-cli
Lightweight PowerShell wrapper to launch Tenable Nessus scans via API. Features automatic X-API-Token extraction from nessus6.js, dynamic session management, and SSL bypass. Fully compatible with PowerShell 5.1 and 7+ environments.
